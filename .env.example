# Example environment variables for TesisFront
# Copy this file to `.env.local` (for local dev) or `.env` (for production deploys)
# and set the values accordingly.

# Backend base URL (no trailing slash)
# In dev, Vite proxy is configured to forward /api to this URL.
# Typical local backend:
VITE_API_URL=http://localhost:8080
